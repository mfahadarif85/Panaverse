/* Adding Comments: Choose two of the programs you’ve written, and add at least one comment to each.
 If you don’t have anything specific to write because your programs are too simple at this point, 
 just add your name and the current date at the top of each program file. 
 Then write one sentence describing what the program does.*/

// stores a name in a variable with white spaces
let userName = `\n\tMuhammad Fahad\n`;
// display the name with whitespaces
console.log(userName);
//display the name after removing whitspaces using trim() method
console.log(userName.trim());
console.log(userName.trim());

// stores favourite number in a variable
const favNum = 85;
// stores message in a variable
const message = `My favourite number is ${favNum}.`;
// Displays message and favourite number in the console
console.log(message);
